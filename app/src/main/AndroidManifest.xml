<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.guardianangelsafetyapp">
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <uses-permission android:name="android.permission.SEND_SMS"/>
    <application

        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity"
                  android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DataActivity" android:theme="@style/AppTheme" android:label="Sensor Data">

        </activity>
        <activity android:name=".ContactsActivity" android:label="Contacts">

        </activity>
        <activity android:name=".AddEditActivity" android:label="Edit Contact" android:theme="@style/AppTheme.NoActionBar">
        
        </activity>

        <activity android:name=".TimerActivity">

        </activity>
        <service android:name=".NotifService" android:enabled="true"/>
        <receiver android:name=".AlarmReceiver" />
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>
</manifest>